🔐 LOGIN & ACCOUNT MANAGEMENT

Keep automatic re-login using previously entered credentials.

Redesign the landing page:

Add a single “Login” button for all users (staff, parents, drivers).

After login, route users to their correct dashboards based on account type.

Remove Twitter and GitHub authentication options (parents should only log in via email/password).

Add a Profile Management section where users can update:

Name

Email

Phone Number

Address

Emergency Contact

If this info is missing, show a banner or prompt at the top linking users to complete their profile.

👨‍👩‍👧 CHILD INFORMATION MANAGEMENT (Parents)

Allow parents to:

Add / Edit / Remove child profiles.

Each child should include:

Name (required)

Age (required)

Height

Race

If no child is registered, show a prompt or button linking directly to the child registration page.

🧭 ROUTE BUILDER (Admins)

Admins can manually create routes by selecting children from a list and setting their order.

Add a status system:

Highlight children not assigned to a route.

Warn the admin before creating a route if a selected child is already on another route.

Add visibility tools so:

Admins can see which children are assigned to which routes.

Parents can view the route their child is on.

🕒 DRIVER TIME TRACKING

Add a Clock In/Clock Out toggle for drivers.

When clocked in:

Show a live timer that runs until they clock out.

Add a failsafe that automatically clocks out drivers who forget after a set time limit.

Allow manual time adjustments but restrict them to a 24-hour window to prevent abuse.

Include this feature in a dedicated Time Tracking page or section.

⚙️ IMPLEMENTATION INSTRUCTIONS

Use role-based dashboards (Admin, Parent, Driver) that share a single unified login.

Update the database schema to store:

User profile info

Child records

Route assignments

Driver time logs

Add conditional UI prompts when required data is missing (e.g., profile incomplete, no child registered).

Implement frontend logic for:

Live timers

Manual time edits

Route selection and warnings

Ensure all changes are secure, persistent, and user-friendly.